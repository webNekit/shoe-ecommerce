/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ["localhost", "shoe-ecommerce-backend-wm0e.onrender.com"]
    }
};

export default nextConfig;
